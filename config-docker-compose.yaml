version: "3.9"

services:
  knock-config:
    build: .
    container_name: knockknock-config
    volumes:
      - "./config:/config"
      - "./run-scripts:/app"
    command: python /app/py/gen_stanza.py :TODO ADD ARGS
    secrets:
      - secret_pw

secrets:
  secret_pw:
    external: true
    name: password